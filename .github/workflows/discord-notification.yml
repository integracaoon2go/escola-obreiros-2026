name: Auto Discord - Materiais

on:
  schedule:
    # Sexta 9h - Materiais dos Módulos
    - cron: '0 12 * * 5'
  push:
    branches: [main]
    paths: ['modulo-*.html', 'index.html']

jobs:
  discord-notifications:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar para #materiais-dos-modulos
        run: |
          curl -X POST ${{ secrets.DISCORD_WEBHOOK_URL }} \
            -H 'Content-Type: application/json' \
            -d '{
              "content": "📚 **APOSTILA SEMANAL DISPONÍVEL**",
              "embeds": [{
                "title": "✅ Novo Material Liberado",
                "description": "Confira a apostila e os materiais desta semana!",
                "color": 3092790,
                "fields": [
                  {"name": "📖 Formato", "value": "PDF + HTML", "inline": true},
                  {"name": "📥 Download", "value": "https://seu-site.com", "inline": true}
                ]
              }]
            }'
